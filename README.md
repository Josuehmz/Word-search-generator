# Word search generator
 The code is a word search puzzle generator that prompts the user for the number of words and the size of the grid, and then asks the user to enter the words to be included in the puzzle. It tries to place each word in a random position and direction in the grid, making sure that the word fits and does not overlap with any other word already placed
